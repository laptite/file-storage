.wrapper-with-padding
	#docs.clearfix
		- unless @docs.blank?
			- @docs.each do |doc|
				= link_to doc do
					.doc
						%p.title= doc.title
						%p.date= time_ago_in_words(doc.created_at)
						%p.content= truncate(doc.content, length: 120)
		- else
			%h2 Nothing here yet!
			%p Start creating documents and organizing your life.
			%button.mt-15= link_to "Create Doc", new_doc_path